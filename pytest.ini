[pytest]
addopts = -m "not slow"
pythonpath = src
testpaths = tests
markers =
    integration: tests that call spglib and use SQLite together
    slow: tests that build or validate the full 1651 MSG database
filterwarnings =
    ignore:Set OLD_ERROR_HANDLING to false and catch the errors directly.:DeprecationWarning:spglib.msg
